# Auto detect text files and normalize line endings
* text=auto

# Force LF line endings for shell scripts (Unix/Linux)
*.sh text eol=lf
*.bash text eol=lf

# Force CRLF for Windows scripts
*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf
